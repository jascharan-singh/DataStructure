public class Heap {

    public void minheap(int [] arr, int value){
        arr[arr.length] = 3;
        for (int i = 0; i < arr.length; i++) {
            System.out.println(arr[i]);
        }
        System.out.println(arr.length);
    }
}
